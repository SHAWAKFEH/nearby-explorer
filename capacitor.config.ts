import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.university.nearbyexplorer',
  appName: 'Nearby Explorer',
  webDir: 'dist'
};

export default config;
